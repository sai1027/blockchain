1. 
	a) YES. InnerFunctin() is a closure.
	b) 100


2. 
	scope : 
		The declared variables are accessed only inside the function.  
	
	Closure :
		If we define a function inside another function the inner function is called a closure.


3.
	Closure function can access outer function variables. simillarly, lexical scope is the 
	environment that holds the variable of the current scope as well as the outer scope.
	

4. output:
	
	3
	3
	3